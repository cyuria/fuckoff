- Can use python features from v3.9 (v3.8 hit EOL on 2024-10-07)
- Refactor `corrector.organize_commands()`
  - use dict keys as set, which is ordered from python v3.7
- Unicode string literals are default in python3, might as well get rid of them
- What's going on in `utils.get_all_matched_commands()`?
- In fact, what's going on in `utils` in general?
- Use formatted string literals (f-strings) from python v3.6+
- Variable type annotations from python v3.6+
- Remove instant mode?
- Remove deprecated properties?
- Rework `conf.py`/settings to be more readable and better support type hints
- Remove support for `FUCKOFF_HISTORY`?
